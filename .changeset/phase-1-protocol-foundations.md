---
---

Establish `@repo/protocol` package: envelope, error, streaming, and tool registry contracts. Deletes the legacy `apps/design-plugin` starter to repurpose the repo for the Figma MCP server product.
