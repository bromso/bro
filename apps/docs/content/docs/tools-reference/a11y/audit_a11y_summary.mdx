---
title: audit_a11y_summary
description: "Walk a node (optionally recursively) and aggregate accessibility checks: contrast, target size, alt text, ARIA label, landmark role. Each check is graded `ok` / `warn` / `error` with a human-readable detail string. The most useful tool when an LLM wants to grade a frame's overall a11y posture in a single call."
---

import { ToolReference } from "@/components/tool-reference";

<ToolReference
  pack="tools-a11y"
  name="audit_a11y_summary"
  description="Walk a node (optionally recursively) and aggregate accessibility checks: contrast, target size, alt text, ARIA label, landmark role. Each check is graded `ok` / `warn` / `error` with a human-readable detail string. The most useful tool when an LLM wants to grade a frame's overall a11y posture in a single call."
  streaming={false}
  input={{"type":"object","properties":{"nodeId":{"type":"string","minLength":1},"recursive":{"type":"boolean","default":false}},"required":["nodeId"],"additionalProperties":false}}
  output={{"type":"object","properties":{"nodeId":{"type":"string"},"checks":{"type":"array","items":{"type":"object","properties":{"name":{"type":"string","enum":["contrast","target_size","alt_text","aria_label","landmark_role"]},"status":{"type":"string","enum":["ok","warn","error"]},"detail":{"type":"string"}},"required":["name","status","detail"],"additionalProperties":false}},"nodesScanned":{"type":"integer","minimum":0}},"required":["nodeId","checks","nodesScanned"],"additionalProperties":false}}
/>

## Description

Walk a node (optionally recursively) and aggregate accessibility checks: contrast, target size, alt text, ARIA label, landmark role. Each check is graded `ok` / `warn` / `error` with a human-readable detail string. The most useful tool when an LLM wants to grade a frame's overall a11y posture in a single call.

## See also

- Pack overview: [tools-a11y](/docs/tools/a11y)
