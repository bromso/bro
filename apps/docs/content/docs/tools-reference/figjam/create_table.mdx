---
title: create_table
description: "FigJam-only. Create a table with the given row + column count."
---

import { ToolReference } from "@/components/tool-reference";

<ToolReference
  pack="tools-figjam"
  name="create_table"
  description="FigJam-only. Create a table with the given row + column count."
  streaming={false}
  input={{"type":"object","properties":{"rows":{"type":"integer","exclusiveMinimum":true,"minimum":0},"columns":{"type":"integer","exclusiveMinimum":true,"minimum":0},"x":{"type":"number"},"y":{"type":"number"},"width":{"type":"number","exclusiveMinimum":true,"minimum":0},"height":{"$ref":"#/properties/width"}},"required":["rows","columns","width","height"],"additionalProperties":false}}
  output={{"type":"object","properties":{"nodeId":{"type":"string"},"type":{"type":"string","enum":["TABLE"]}},"required":["nodeId","type"],"additionalProperties":false}}
/>

## Description

FigJam-only. Create a table with the given row + column count.

## See also

- Pack overview: [tools-figjam](/docs/tools/figjam)
