---
title: delete_webhook
description: "REST. Delete a webhook. WRITE — gated behind --enable-write-tools (default off)."
---

import { ToolReference } from "@/components/tool-reference";

<ToolReference
  pack="tools-rest"
  name="delete_webhook"
  description="REST. Delete a webhook. WRITE — gated behind --enable-write-tools (default off)."
  streaming={false}
  input={{"type":"object","properties":{"webhookId":{"type":"string","minLength":1}},"required":["webhookId"],"additionalProperties":false}}
  output={{"type":"object","properties":{"ok":{"type":"boolean","enum":[true]}},"required":["ok"],"additionalProperties":false}}
/>

## Description

REST. Delete a webhook. WRITE — gated behind --enable-write-tools (default off).

## See also

- Pack overview: [tools-rest](/docs/tools/rest)
