---
title: VS Code Copilot
description: Install figma-mcp into VS Code Copilot and verify the connection.
---

## Config path

- macOS: `~/Library/Application Support/Code/User/mcp.json`
- Linux: `~/.config/Code/User/mcp.json`
- Windows: `%APPDATA%\Code\User\mcp.json`

This is the user-scoped MCP config that VS Code Copilot reads. The setup CLI writes the `mcpServers.figma` entry into it.

## What `setup` writes

```json
{
  "mcpServers": {
    "figma": {
      "command": "npx",
      "args": ["-y", "@bromso/figma-mcp"]
    }
  }
}
```

`setup` preserves any sibling entries in `mcpServers` and any other top-level keys in the file. The write is atomic (temp file plus rename), so a partial write can never corrupt the config.

## Reload behavior

The new entry takes effect on the next chat session. To force-reload immediately, run **Cmd-Shift-P → Developer: Reload Window**.

## Verify

```bash
figma-mcp doctor --json | jq '.checks[] | select(.id == "ai-client-configs")'
```

This confirms the entry is present and that the binary on `PATH` matches the configured command.

## Troubleshooting

- Entry written but not visible — see [config drift](/docs/troubleshooting#ai-client-configs-config-drift).
- Tool call hangs — see [plugin not paired](/docs/troubleshooting#plugin-pairing-plugin-not-paired).
- Daemon errors after a VS Code update — see [stale lockfile](/docs/troubleshooting#daemon-liveness-stale-lockfile-dead-daemon).
